import { IGuayabaConfig } from "@guayaba/core"
import { ModelEntity } from "../database/models/entity.model"

export class BuilderFacade {

  constructor(model: ModelEntity, config: IGuayabaConfig & { path: string }) {

  }

  async invoke() {
    try {

    } catch (error) {

    } finally {

    }
  }
}
